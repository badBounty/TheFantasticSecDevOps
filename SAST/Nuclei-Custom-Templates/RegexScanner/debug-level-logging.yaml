id: debug-level-logging

info:
  name: Debug level set for logging
  author: badBounty
  severity: medium
  tags: logs,RegexScanner

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - ".*level value.*=.*debug.*"
